Librarie:
	int		id (PK) (FK)
	string 	name
	(string?)	map_adress
	boolean	Full
	int 		max_slots
	int 		current_occupied_slots
	image		photo
	ArrayList<Book.id>(many to many*)	books

Book: 
	int 	id (PK)(FK) // NOTE: É key comoposta porque podem haver varios do mesmo livro 
	string Title (PK)(FK)
	image	photo
	ArrayList<Librarie.id>(many to many*)	libraries

User:
	int 	id (PK) (FK)
	ArrayList<Librarie.id>(one to many)	addresses
	ArrayList<book.id>(one to many)	activeNotifs